# Cloudflare Pages build settings
# These match what you enter in the Cloudflare dashboard

[build]
command = "npm run build"
output_directory = "dist"

# SPA routing — redirect all 404s to index.html
[[redirects]]
from = "/*"
to = "/index.html"
status = 200
